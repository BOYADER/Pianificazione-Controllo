#!/usr/bin/env python

class Waypoint:
	def __init__(self, latitude, longitude, depth, tolerance):
		self.latitude = latitude
		self.longitude = longitude
		self.depth = depth
		self.tolerance = tolerance

